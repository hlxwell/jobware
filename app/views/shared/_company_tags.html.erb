<fieldset class="big">
  <legend>公司标签</legend>
  <% tag_cloud(Company.tag_counts_on(:tags), %w(css1 css2 css3 css4)) do |tag, css_class| %>
    <%= link_to tag.name, companies_path(:tag => tag.name), :class => css_class %>
  <% end %>
</fieldset>